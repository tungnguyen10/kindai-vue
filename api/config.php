<?php 

// Define DB Name
define( 'DB_NAME', 'kindai' );

// Define DB User
define( 'DB_USER', 'root' );

// Define DB Password
define( 'DB_PASSWORD', '' );

// Define DB Host
define( 'DB_HOST', 'localhost' );

// Define DB DNS
define( 'DB_DSN', 'mysql:dbname=kindai;host=localhost' );

// Define id length
define( 'ID_LENGTH', 12);